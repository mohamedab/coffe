export class Product {
  name!: string; // Use "!" to indicate it will be initialized later
  price!: number; // Use "!" to indicate it will be initialized later
  image!: string; // Use "!" to indicate it will be initialized later
  ingredients!: string[]; // Use "!" to indicate it will be initialized later
  quantity!: number;
  total!: number;

}
