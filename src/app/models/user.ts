export class User {
  userId!: string;     // Unique user ID
  name!: string;       // User's name
  email!: string;      // User's email address
  roles!: string[];    // Array of roles (e.g., 'admin', 'customer', 'server')
}
